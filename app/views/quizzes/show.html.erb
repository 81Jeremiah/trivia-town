
<% if @quiz.user_id == current_user.id %>
<% @quiz.question_and_answers.each do |qa| %>
  <ol>
    <li>Question: <%= qa.question %>
        <br>
        Answer: <%= qa.answer %></li>
    </ol>
    <% end %>
  <%= link_to "Edit Quiz", edit_quiz_path %>
 <% end %>


 <%= @quiz.name %>
  Would you like to <%=link_to "take this quiz", new_quiz_game_path(@quiz.id) %>



<% quiz = @quiz %>
Top Scores:
<ol>
<% Game.top_5_scores(quiz).each do |game| %>

<li>Score: <%= game.score %>
User: <%= game.user.username %></li>

<% end %>
</ol>

Comments
