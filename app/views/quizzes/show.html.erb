
<% if @quiz.user_id == current_user.id %>
<% @quiz.question_and_answers.each do |qa| %>
  <ol>
    <li>Question: <%= qa.question %>
        <br>
        Answer: <%= qa.answer %></li>
    </ol>
    <% end %>
  <%= link_to "Edit Quiz", edit_quiz_path %>
 <% end %>

 <div class="center">

 <h1> <%= @quiz.name %> </h1>
 <p>Categories: <% @quiz.categories.each do|category| %>
    <%= category.name %>
    <% end %></p>
  <h3><strong> Would you like to <%=link_to "take this quiz", new_quiz_game_path(@quiz.id) %> <h3>
</div>

 <% quiz = @quiz %>
<h2>Top Scores:</h2>

<table class="table">
  <thead>
    <tr>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><strong>User Name</strong></td>
      <td><strong>Score</strong></td>
    </tr>
    <ol>
    <% Game.top_5_scores(quiz).each do |game| %>

    <li>User: <%= game.user.username %>
    Score: <%= game.score %></li>
    <% end %>
    </ol>
      <tr>

  </tbody>
</table>
<br>
<p>Comments: </p>

<p><% @quiz.comments.each do |comment| %>
    <%= link_to comment.user.username, user_path(comment.user) %> said
    <%= comment.content %>
  <% end %></p>

<<%= simple_form_for [@quiz, @quiz.comments.build] do |f| %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>

